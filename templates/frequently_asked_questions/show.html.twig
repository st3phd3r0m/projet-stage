{% extends 'base.html.twig' %}

{% block title %}FrequentlyAskedQuestions{% endblock %}

{% block body %}
    <h1>FrequentlyAskedQuestions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frequently_asked_question.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ frequently_asked_question.question }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ frequently_asked_question.answer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('frequently_asked_questions_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('frequently_asked_questions_edit', {'id': frequently_asked_question.id}) }}" class="btn btn-warning" >Modifier</a>

    {{ include('frequently_asked_questions/_delete_form.html.twig') }}
{% endblock %}
