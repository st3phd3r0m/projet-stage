{% extends 'base-admin.html.twig' %}

{% block title %}Attributes{% endblock %}

{% block body %}
    <h1>Attributes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attribute.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ attribute.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ attribute.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attributes_index') }}">back to list</a>

    <a href="{{ path('attributes_edit', {'id': attribute.id}) }}">edit</a>

    {{ include('attributes/_delete_form.html.twig') }}
{% endblock %}
