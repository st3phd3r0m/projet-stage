{% extends 'base-admin.html.twig' %}

{% block title %}Ludo et Sophie - Voir attribut
	{{ attribute.id  }}
{% endblock %}

{% block body %}
	<!-- Content Header (Page header) -->
	<div class="content-header">
		<div class="container-fluid">
			<div class="row mb-2">
				<div class="col-sm-6">
					<h1 class="m-0 text-dark">Ludo et Sophie - Voir attribut
						{{ attribute.id  }}</h1>
				</div>
				<!-- /.col -->
				<div class="col-sm-6">
					<ol class="breadcrumb float-sm-right">
						<li class="breadcrumb-item">
							<a href="{{ path('admin') }}">Dashboard</a>
						</li>
						<li class="breadcrumb-item active">Attributs</li>
					</ol>
				</div>
				<!-- /.col -->
			</div>
			<!-- /.row -->
		</div>
		<!-- /.container-fluid -->
	</div>
	<!-- /.content-header -->

	<!-- Main content -->
	<div class="content">
		<div class="container-fluid">

			<table class="table">
				<tbody>
					<tr>
						<th>Id</th>
						<td>{{ attribute.id }}</td>
					</tr>
					<tr>
						<th>Nom</th>
						<td>{{ attribute.name }}</td>
					</tr>
					<tr>
						<th>Valeur</th>
						<td>{{ attribute.value }}</td>
					</tr>
				</tbody>
			</table>

			<a href="{{ path('attributes_index') }}">Retour à la liste</a>

			<a href="{{ path('attributes_edit', {'id': attribute.id}) }}">Modifier</a>

			{{ include('attributes/_delete_form.html.twig') }}

		</div>
	</div>
{% endblock %}
