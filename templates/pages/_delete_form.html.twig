{# <form method="post" action="{{ path('pages_delete', {'id': page.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page.id) }}">
    <button class="btn">Supprimer</button>
</form> #}

<!-- Button trigger modal -->
<button class="btn btn-danger" data-toggle="modal" data-target="#deletePage{{ page.id }}ModalCenter">
  <i class="fas fa-trash"></i>
</button>

<!-- Modal -->
<div class="modal fade" id="deletePage{{ page.id }}ModalCenter" tabindex="-1" role="dialog" aria-labelledby="deletePage{{ page.id }}ModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deletePage{{ page.id }}ModalLongTitle">Suppression de la publication <span class="font-italic">{{ page.title }}</span></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                La suppression de la publication <span class="font-italic">{{ page.title }}</span> sera irréversible.
                Voulez-vous vraiment supprimer cette publication ?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
                <form method="post" action="{{ path('pages_delete', {'id': page.id}) }}">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page.id) }}">
                    <button class="btn btn-danger">Supprimer</button>
                </form>
            </div>
        </div>
    </div>
</div>