<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="shortcut icon" href="{{ asset('images/icones/logo_footer.jpg') }}" type="image/x-icon">
        <title>{% block title %}Ludo et Sophie Accueil{% endblock %}</title>

        {# Chargement CDNs #}        
        <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="{{ asset('css/normalize.css') }}">
        <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>


    <div class="container">
    
        <header>

            <div class="container">
                <div class="row mt-5 mt-lg-0">
                    <div class="d-none d-md-block col-md-2 mt-0 px-0">
                        {# Boucle sur liens header #}
                        {% for link in links()|filter( link => link.position == 'header' )|sort((a, b) => a.positionOrder <=> b.positionOrder) %}
                                <a class="blue text-decoration-none" href="{{ link.url }}" 
                                title="{{ link.title }}"
                                target="{{ (link.type == "external") ? '_blank' : '_self' }}">
                                    {% if link.image is not null %}
                                        <img class="d-block mx-auto img-fluid mt-5" src="{{ asset('images/icones/'~ link.image )| imagine_filter('newsletter') }}" alt="Icone du lien {{ link.title }}">
                                    {% endif %}
                                    <p class="d-none d-lg-block m-0 p-0 text-center">{{ link.content }}</p>
                                </a>
                        {% endfor %}
                        {# Fin Boucle sur liens header #}

                    </div>
                    {# Image header #}
                    <div class="col-md-8 mt-5">
                        <a href="{{ path('home') }}" title="Ludo et Sophie : Accueil" >
                            <img class="d-block img-fluid w-50 mx-auto" src="{{ asset('images/icones/nouveaulogo2.png') }}" alt="Image d'accueil du site Ludo et Sophie">
                        </a>
                        <h1 class="font-italic"><span class="red">et la culture devient</span> <span class="blue">un jeu d'***REMOVED***</span><span class="orange">(s)</span><span class="blue">!</span></h1>
                    </div>
                    {# Fin Image header #}
                    <div class="d-none d-md-block col-md-2 mt-0">
                        <div class="row mt-5">
                            {% for foreignLanguage in foreignLanguages() %} 
                            <div class="col-lg-6 p-1">
                                <a href="{{ path('foreign', { slug: foreignLanguage.slug}) }}" title="English and Spanish family tours">
                                    <img class="d-block mx-auto img-fluid" src="{{ asset('images/languages/' ~ foreignLanguage.flag ) | imagine_filter('flag') }}" alt="Icone de la langue {{ foreignLanguage.name }}">
                                </a>
                            </div>
                            {% endfor %}
                            <p class="d-none d-lg-block text-center">English and Spanish family tours</p>                        
                        </div>

                    </div>
                </div>
            </div>

        
            <div class="container">

                <nav class="navbar navbar-expand-lg navbar-light bg-white" id="navbar">

                    <div class="fixed-top d-lg-none" id="burgerAndBannerWrapper">
                    
                        <button class="navbar-toggler p-2 border my-2 mx-2" type="button" data-toggle="offcanvas">
                            <span class="navbar-toggler-icon"></span>
                        </button>   
                        <img id="logoMobile" src="{{ asset('images/icones/logo_mobile.png' ) }}" alt="">
                    
                    </div>


                    <div class="navbar-collapse offcanvas-collapse" id="navbarUL">


                        <ul class="navbar-nav mx-auto">

                            {% set dropDownButtonText = '' %}

                            {# Boucle sur liens nav #}
                            {% for link in links()|filter( link => link.position == 'nav' )|sort((a, b) => a.positionOrder <=> b.positionOrder) %}
                                {% if loop.last %}
                                    {% set dropDownButtonText = link.content  %}

                                <li id="lastDesktop" class="nav-item d-none d-lg-inline mx-lg-4">
                                    <a class="nav-link px-2" 
                                    href="{{ link.url }}" 
                                    title="{{ link.title }}" 
                                    target="{{ (link.type == "external") ? '_blank' : '_self' }}" >
                                        {{ link.content }}
                                    </a> 
                                </li>
                                <li class="nav-item dropdown d-lg-none mx-lg-4">
                                    <a class="nav-link dropdown-toggle px-2" 
                                        data-toggle="dropdown" 
                                        href="#" role="button" 
                                        aria-haspopup="true" 
                                        aria-expanded="false">
                                        {{ link.content }}
                                    </a>
                                    <div class="dropdown-menu" aria-labelledby="dropdown01">
                                        {% for link in links()|filter( link => link.position == 'dropdown' )|sort((a, b) => a.positionOrder <=> b.positionOrder) %}
                                            <a class="dropdown-item px-2" 
                                                href="{{ link.url }}" 
                                                title="{{ link.title }}" 
                                                target="{{ (link.type == "external") ? '_blank' : '_self' }}" >
                                                {{ link.content }}
                                            </a> 
                                        {% endfor %}
                                    </div>
                                </li>
                                {% else %}
                                <li class="nav-item mx-lg-4">
                                    <a class="nav-link px-2" 
                                    href="{{ link.url }}" 
                                    title="{{ link.title }}" 
                                    target="{{ (link.type == "external") ? '_blank' : '_self' }}" >
                                        {{ link.content }}
                                    </a> 
                                </li>
                                {% endif %}

                            {% endfor %}
                            {# Fin Boucle sur liens nav #}

                            {# Boucle sur liens header #}
                            {% for link in links()|filter( link => link.position == 'header' )|sort((a, b) => a.positionOrder <=> b.positionOrder) %}
                                <li class="nav-item d-lg-none">
                                    <a class="nav-link px-2" 
                                        href="{{ link.url }}" 
                                        title="{{ link.title }}"
                                        target="{{ (link.type == "external") ? '_blank' : '_self' }}">
                                        {{ link.content }}
                                    </a>
                                </li>        
                            {% endfor %}
                            {# Fin Boucle sur liens header #}

                        </ul>
                    </div>
                </nav>        
            
            
            </div>

        </header>
    
        <span id="scrollChrevron" class="p-2 m-0 border rounded" title="Aller vers le haut de la page">
            <i class="fa fa-chevron-up"></i>
        </span>        



        <main>

            <div class="container">

                {% for message in app.flashes('success') %}
                    <div class="alert alert-success" role="alert">{{ message }}</div>
                {% endfor %}

                {% for message in app.flashes('fail') %}
                    <div class="alert alert-warning" role="alert">{{ message }}</div>
                {% endfor %}

                {% block body %}{% endblock %}    
            </div>
    
        </main>

        <footer class="border-top"> 
            <div class="container">
                <div class="row">
                    <div class="col-md-3 my-5">
                        <h4 class="mb-4 red font-weight-bold">Une question ? Une idée ?</h4>
                        <div class="row">
                            <div class="col-6 col-md-12 col-xl-6">
                                <img class="img-fluid" src="{{ asset('images/icones/logo_footer.jpg')| imagine_filter('miniatures') }}" alt="Logo footer Ludo et Sophie">                            
                            </div>
                            <address class="col-6 col-md-12 col-xl-6 text-left p-0 m-0 ml-md-4 ml-xl-0">
                                <ul class="p-0 m-0 mt-3">
                                    <li>Ludo &amp; Sophie SAS</li>
                                    <li>49 rue de Ponthieu</li>
                                    <li>75008 Paris</li>
                                </ul>            
                            </address>             
                        </div>
                    </div>
                    <div class="col-md-9 my-5">
                        {{ include('_formMessages.html.twig') }}
                    </div>
                </div>

                <ul>
                    {# Boucle sur liens footer #}
                    {% for link in links()|filter( link => link.position == 'footer' )|sort((a, b) => a.positionOrder <=> b.positionOrder) %}
                        <li class="text-center d-lg-inline-block my-lg-3 align-text-top">
                            <a class="text-decoration-none blue"
                            href="{{ link.url }}" 
                            title="{{ link.title }}" 
                            target="{{ (link.type == "external") ? '_blank' : '_self' }}" >
                                {{ link.content }}
                            </a> 
                        </li>
                    {% endfor %}
                    {# Fin Boucle sur liens footer #}
                </ul>

            
                <p class="text-center blue my-5">Copyright &copy; 2016-2020 Ludo&amp;Sophie. Tous droits réservés.</p>
            </div>
        
        </footer>    
    </div>



        <!-- JS, Popper.js-->
        <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js" integrity="sha384-9/reFTGAW83EW2RDu2S0VKaIzap3H66lZH81PoYlFhbGU+6BZp6G7niu735Sk7lN" crossorigin="anonymous"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8/KUEfYiJOMMV+rV" crossorigin="anonymous"></script>

        <script src="{{ asset('js/miscellaneous.js') }}"></script>

        {% block javascripts %}{% endblock %}
    </body>
</html>



